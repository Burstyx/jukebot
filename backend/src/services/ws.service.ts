import { WebSocket } from "@fastify/websocket";

export default class WSHandler {
    static async hello(socket: WebSocket) {
        console.log("qlq a dit hello trop kikiiiiiiii ♥ ♥ ♥ !!!!!!");
    }
}